# Local development config for a self-hosted LiveKit server.
# You can also run `livekit-server --dev`, which uses devkey/secret.
port: 7880
bind_addresses:
  - "127.0.0.1"

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100

keys:
  devkey: secret

logging:
  level: info
