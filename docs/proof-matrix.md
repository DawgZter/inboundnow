# Proof Matrix

This is the public source of truth for what InboundNow currently proves.

Status values:

- `verified`: exercised locally with a committed smoke, browser run, or test.
- `configured`: code path exists, but the full runtime proof has not been captured.
- `stub`: deterministic local stub proves wiring only.
- `not_proven`: target behavior is not yet proven.
- `forbidden`: behavior is intentionally out of bounds for this MVP.

| Subsystem | Status | Evidence | Boundary |
| --- | --- | --- | --- |
| inboundnow browser layer action bus | verified | `npm test`, browser smokes, `packages/action-protocol` | Widget owns visible cursor, scroll, highlight, click, and Cal UI. |
| Action protocol validation | verified | `test/action-protocol.test.mjs` | Main router/planner path emits primitive actions; `payrollFlow` remains only as a deprecated lab macro. |
| Cal confirmation gate | verified | `npm run smoke:browser:cal-gate`, browser smoke on bridge and LiveKit paths | Cal iframe `src` is empty before confirmation and set only after explicit confirmation. A dismissed prompt re-prompts instead of counting as consent. Generic `navigate` to Cal.com and booking CTA clicks are blocked into the confirmation prompt. |
| LiveKit token/config/SDK asset | verified | `npm run smoke:local`, `npm run smoke:livekit` | Local dev JWTs only; not LiveKit Cloud. |
| Token server security guardrails | verified | `test/token-server.test.mjs` | Loopback-only host/LiveKit URL, hostile CORS rejection, bridge disable, JWT claims/signature, and bridge payload cap. |
| LiveKit room/data-channel control | verified | `npm run smoke:livekit`, browser LiveKit smoke with `ENABLE_SIM_BRIDGE=0` | Proves control/data messages, not ASR or TTS. |
| Browser LiveKit connection | verified | Browser smoke on `http://127.0.0.1:4193/direct` with bridge disabled | Browser joined local room and received `agent.action` over LiveKit. |
| Browser mic publication and auto turn lifecycle | configured | `connectLiveKitRoom()`, browser mic proof events, `Start AI Persona` auto-stop controller | Browser emits publication/start/stop media proof and Start AI Persona owns the turn stop lifecycle in the H100 harness. Still not ASR proof by itself; permission behavior and media stats vary by browser. |
| Synthetic LiveKit audio to ASR adapter | verified | `npm run smoke:asr:livekit` | Publishes a synthetic local microphone track into LiveKit, buffers it with `AudioStream(track)`, sends WAV audio to a fake localhost Parakeet endpoint, then drives `agent.asr.final` -> answer -> action. Not real browser microphone or real model proof. |
| Browser ASR UI state | verified | `npm run smoke:browser:asr-ui` | Browser widget shows current ASR boundary copy, listening/no-audio states, final transcript fallback, preserved voice profile, caption-only streamed speech, local TTS audio events, stale-audio ignore, and PCM scheduling after an ASR final transcript. |
| Browser final transcript input | verified | `npm run smoke:asr:local`, `prospect.transcript.final` | Final transcript messages can drive retrieval/planning/actions; this is transcript fallback proof, not local Parakeet audio proof. |
| WebSocket fallback bridge | verified | `npm run smoke:local` | Simulated fallback for local dev only; not the target transport. |
| Local Parakeet ASR adapter contract | configured | `test/asr-local-parakeet.test.mjs`, `npm run smoke:asr:local`, `npm run smoke:asr:livekit` | Localhost-only adapter and fake endpoint smokes prove request/response wiring, including synthetic LiveKit audio capture, but no real Parakeet model audio proof. |
| Parakeet ASR model audio | not_proven | `scripts/vast-h100/start-parakeet-asr.sh`, `scripts/vast-h100/smoke-parakeet-endpoint.mjs` | Requires H100-local `nvidia/parakeet-tdt-0.6b-v3` endpoint and real audio file proof before claiming ASR. |
| Qwen 3.6 27B local LLM | not_proven | `qwen-stub`, `qwen-openai-local` adapter, `scripts/vast-h100/start-qwen-vllm.sh` | Local OpenAI-compatible endpoint support exists and defaults to `Qwen/Qwen3.6-27B`; real vLLM/SGLang completion not captured. |
| Qwen H100 endpoint anti-stub guard | configured | `scripts/vast-h100/smoke-qwen-endpoint.mjs`, `scripts/vast-h100/smoke-browser-persona-h100.mjs` | H100 smokes query `/v1/models` and reject stub/fake/fixture model ids or owners before accepting planner proof. This guard is code-level until run against vLLM/SGLang on the H100. |
| LLM planner JSON validation | verified | `test/llm-planner.test.mjs`, `npm run smoke:planner` | Proves strict JSON/action validation, deprecated macro rejection, fail-closed proof mode, and deterministic fallback against a local stub, not real Qwen reasoning. |
| H100 proof-mode worker gates | verified | `apps/agent/proof-mode.mjs`, `test/proof-mode.test.mjs` | In `H100_PROOF_MODE=1`, startup requires LiveKit verified/real mode plus local Parakeet, Qwen, Moss runtime client, and Miso One. Per-turn proof rejects transcript/stub/simulated ASR, missing/errored/simulated/non-local/empty Moss, planner fallback, and contract-only TTS before answer/action. This verifies guard behavior only, not a captured real H100 run. |
| Browser streamed speech fallback | verified | `npm run smoke:local`, `npm run smoke:livekit`, `npm run smoke:browser:asr-ui` | Worker emits `agent.speech.start/chunk/end`; browser queues chunks through `speechSynthesis` when model audio is unavailable, or displays caption-only chunks when model audio is active. This is streamed fallback/caption proof, not Miso One model proof. |
| Dynamic voice switching | verified | `test/voice-session.test.mjs`, `npm run smoke:voice:switching` | Per-session voice profile changes propagate over bridge/agent messages and streamed speech metadata; browser fallback voice/rate/pitch changes are not model-audio proof. |
| Local VibeVoice TTS adapter contract | verified | `npm run smoke:tts:local`, `npm run smoke:tts:agent`, `npm run smoke:browser:asr-ui`, `apps/agent/adapters/tts/local-vibevoice.mjs` | Localhost-only VibeVoice-compatible endpoint boundary with prewarm, cache key, dtype, LLM-only quantization metadata, `agent.tts.start/chunk/end`, browser PCM16 scheduling, stale-audio ignore, and contract-level proof metadata. Fake endpoint smoke is not model proof and keeps `localVibeVoiceProven: false`. |
| VibeVoice model audio | not_proven | `scripts/vast-h100/smoke-vibevoice-endpoint.mjs` | Legacy compatibility lane only; requires an H100-local compatible endpoint plus browser proof before claiming real audio. |
| Miso One TTS adapter contract | configured | `apps/agent/adapters/tts/local-miso-one.mjs`, `scripts/vast-h100/start-miso-one-tts.sh`, `scripts/vast-h100/smoke-miso-one-endpoint.mjs` | Localhost-only MisoTTS endpoint wrapper and smoke command exist. No H100-generated Miso One audio is captured yet. The public wrapper defaults to `TTS_QUANTIZATION=none` and reports quantization/LoRA loading as unsupported until implemented. |
| Miso One LoRA development lane | configured | `docs/miso-lora-runbook.md`, `configs/miso-lora/manifest.example.json`, `packages/miso-lora` | Manifest validation, H100 setup, launch wrapper, runtime metadata, and fail-closed `MISO_REQUIRE_LORA` clone-proof gate only. No trained LoRA adapter, loader, `loraAdapterApplied: true`, or cloned Miso One audio is proven. |
| Moss fixture retrieval | verified | `npm run smoke:adapters`, `npm run smoke:local`, `npm run smoke:livekit` | Fixture retrieval proves wiring, not local artifact or hosted Moss runtime proof. |
| Local retrieval artifact through Moss boundary | verified | `npm run smoke:moss:local` | Queries a prebuilt local JSON artifact through the local runtime/client path; not hosted Moss or Moss SDK proof. Runtime must not use `autoRefresh`, cloud polling, `pushIndex()`, runtime/session document upload, or session embedding upload. |
| Remote.com retrieval corpus | verified | `npm run moss:index:remote`, `npm run smoke:moss:remote` | Indexes the committed 10,842-document compressed Remote.com corpus into a local artifact and queries it through the local Moss runtime/client path. Not hosted Moss or Moss SDK runtime proof; raw fetch payloads, crawl logs, job records, and loose scrape files are omitted from git. |
| Hosted Moss index creation for Remote.com scrape | verified | `moss index get remote-com-2026-06-07`, `moss query remote-com-2026-06-07 ...` | Uploaded 10,842 Remote.com scrape documents to Moss index `remote-com-2026-06-07` (`4536a03c-8275-4709-a5d8-768725c220a6`) with model `moss-minilm`. This is hosted index-generation/query proof only; local MVP runtime still must not depend on hosted Moss. |
| LiveKit agent with Remote.com retrieval | verified | `npm run smoke:livekit:moss-remote` | Builds the 10,842-document Remote.com local artifact from the committed gzip bundle, serves it through the local Moss runtime, runs the LiveKit agent with `MOSS_PROVIDER=local-runtime-client`, and verifies the answer/action path carries bounded Remote snippets and local artifact metadata. Not hosted Moss or real LLM proof. |
| Vast.ai H100 local-model lane | configured | docs/vast-h100-runbook.md, scripts/vast-h100/ | Requires H100 preflight plus Qwen/ASR/TTS smokes before any real-model claim. |
| H100 proof-suite manifest | configured | `npm run smoke:h100:proof-suite`, `scripts/vast-h100/run-proof-suite.mjs` | Umbrella runner creates one manifest across Remote.com local Moss, Qwen, Parakeet, Miso, model-chain, and browser persona proof artifacts. It is configured only until run on the real Vast.ai H100 stack with committed `manifest.json` evidence. |
| H100 browser persona proof harness | configured | `npm run smoke:h100:browser-persona`, `scripts/vast-h100/smoke-browser-persona-h100.mjs` | Executable browser harness clicks the visible Start AI Persona control and fails closed on missing LiveKit/mic, bridge fallback, direct smoke-only turn stop, stubs, simulated ASR, planner fallback, contract-only TTS, missing browser mic proof, missing worker audio proof, ASR hash mismatch, `payrollFlow`, and pre-confirmation Cal loads. It saves `browser-mic-proof.json` and `proof-chain.json` with sanitized metadata. It has not been run against the real Vast.ai H100 stack yet. Automated `BROWSER_MIC_AUDIO_PATH` mode is browser media-fixture proof, not manual human speech proof. |
| Hosted Moss runtime behavior | forbidden | README hard constraint | No hosted runtime behavior in local MVP. |
| Production native desktop control | forbidden | README hard constraint | Production visitors must be browser-native. |
| Stagehand execution | forbidden for MVP | README hard constraint | Stagehand may resolve selectors later but must not execute visible UI. |

## Latest Evidence

- `npm run check` passed on June 7, 2026, including 69 tests, H100 proof-mode helper tests, primitive-action planner/router validation, deprecated `payrollFlow` rejection in planner proof mode, the local Miso One adapter unit tests, adapter/planner/Moss/ASR/TTS/voice smokes, the agent local TTS audio-event smoke, the browser Cal gate smoke with navigate/CTA bypass checks, browser ASR UI smoke, H100 proof-suite syntax validation, and Miso LoRA manifest validation.
- `H100_PROOF_SUITE_DRY_RUN=1 npm run smoke:h100:proof-suite` passed on June 7, 2026, proving the proof-suite manifest wiring and required-input checks can run locally. This is dry-run validation only, not real H100 model proof.
- Browser auto-turn code is configured in the website lab: Start AI Persona starts a browser mic turn, the browser-side analyser can auto-stop on silence/timeout, and proof events capture mic publication/start/stop metadata. This remains configured until the H100 browser harness produces a fresh non-dry-run artifact.
- `npm run smoke:local` passed at `artifacts/smoke/2026-06-07T13-34-09-339Z/result.json`, including `streamedSpeechEvents: true`.
- `npm run smoke:livekit` passed with `bridgeDisabled: true` at `artifacts/smoke/livekit-2026-06-07T13-34-10-059Z/result.json`, including `dataChannelSpeechStream: true`.
- `npm run smoke:planner` passed against a temporary local Qwen-compatible stub, proving strict planner JSON parsing and validated action dispatch without real Qwen proof.
- `npm run smoke:moss:local` passed at `artifacts/smoke/moss-local-2026-06-07T13-33-16-986Z/result.json`, proving `provider: local-artifact`, `localOnly: true`, `simulated: false`, local runtime health, direct runtime query, and agent `local-runtime-client` query against a deterministic local artifact.
- `npm run smoke:moss:remote` passed at `artifacts/smoke/moss-remote-2026-06-07T13-29-46-759Z/result.json`, proving a 10,842-document Remote.com corpus local artifact, local runtime health, bounded Remote excerpts, direct runtime query, and agent `local-runtime-client` query.
- Hosted Moss upload completed for the 2026-06-07 Remote.com scrape source artifact: index `remote-com-2026-06-07`, id `4536a03c-8275-4709-a5d8-768725c220a6`, status `Ready`, `doc_count: 10842`, model `moss-minilm`. A local Moss query for `How does Remote help with global payroll?` returned Remote payroll/global HR documents.
- `npm run smoke:livekit:moss-remote` passed at `artifacts/smoke/livekit-moss-remote-2026-06-07T13-29-04-783Z/result.json`, proving a local LiveKit agent turn used the Remote.com corpus-backed Moss runtime client with bridge disabled, streamed speech events, and typed browser action dispatch. Current code now emits primitive actions instead of the deprecated `payrollFlow` macro on the main path.
- `npm run smoke:asr:local` passed at `artifacts/smoke/asr-local-2026-06-07T13-33-17-616Z/result.json`, proving `prospect.transcript.final` can drive the agent/action path and a fake localhost Parakeet-compatible endpoint can return a transcript into the same path. This is not real Parakeet model proof.
- `npm run smoke:asr:livekit` passed at `artifacts/smoke/livekit-asr-local-2026-06-07T13-33-18-410Z/result.json`, proving a synthetic LiveKit microphone track reached the worker `AudioStream(track)` buffer, encoded a WAV, hit the fake localhost Parakeet endpoint, emitted `agent.asr.final` with `source: livekit-audio-turn`, and drove answer/action. This is not real browser microphone or H100 Parakeet proof.
- `npm run smoke:browser:asr-ui` passed at `artifacts/smoke/browser-asr-ui-2026-06-07T13-34-06-525Z/result.json`, proving the browser ASR chips/statuses update for listening, no-audio, and final transcript fallback; transcript display includes `Heard: ...`; the selected voice profile survives ASR final and answer events; and streamed speech starts.
- `npm run smoke:tts:local` passed at `artifacts/smoke/tts-streaming-2026-06-07T13-34-03-341Z/result.json`, proving the localhost streaming TTS adapter contract, prewarm call, stable cache key, `style`, `loraAdapter`, `bfloat16`, and `llm-int8` LLM-only quantization metadata against a fake local endpoint only.
- `npm run smoke:tts:agent` passed at `artifacts/smoke/agent-tts-local-2026-06-07T14-04-22-864Z/result.json`, proving the worker prewarms and streams from a local VibeVoice-compatible endpoint, emits `agent.tts.start/chunk/end`, uses 24 kHz PCM16 audio chunks, marks the proof as `contract`, keeps `localVibeVoiceProven: false`, and lets page actions overlap model-audio streaming for latency.
- `npm run smoke:browser:asr-ui` passed at `artifacts/smoke/browser-asr-ui-2026-06-07T14-04-27-938Z/result.json`, proving browser caption-only speech for model audio, local PCM16 chunk scheduling, `proofLevel: contract`, stale interrupted TTS chunk rejection, and out-of-order/duplicate chunk handling.
- `npm run smoke:voice:switching` passed at `artifacts/smoke/voice-switching-2026-06-07T13-34-03-702Z/result.json`, proving a per-session switch to `warm`, streamed speech voice metadata, and warm-profile persistence on the next bridge turn.
- `npm run smoke:browser:cal-gate` passed at `artifacts/smoke/browser-cal-gate-2026-06-07T15-41-21-755Z/result.json`, proving the embedded browser widget keeps the Cal iframe `src` empty on load, blocks direct `confirmBooking`, defers `openCal`, blocks generic Cal.com navigation and booking CTA clicks into the confirmation prompt, re-prompts after a dismissed prompt, and loads Cal only after clicking `Yes, open Cal`.
- `npm run miso:lora:validate` passed at `artifacts/miso-lora/validation/miso-one-lora-dev.validation.json`, proving the checked-in Miso One LoRA manifest satisfies local-only consent and path guardrails.
- Browser planner smoke passed on June 7, 2026 with `AGENT_PLANNER=local-llm`, local Qwen-compatible stub, and bridge disabled: the proof line showed `Planner local-llm-json via qwen-openai-local`, booking prompt opened, and Cal stayed unloaded before confirmation.
- Browser LiveKit smoke passed on June 7, 2026: the local persona UI joined local LiveKit with `ENABLE_SIM_BRIDGE=0`, received a LiveKit `agent.action`, the page scrolled/highlighted, Cal stayed unloaded before confirmation, and Cal opened after confirmation.
